<ul class="cards">
  <slot />
</ul>

<style>
  @layer components {
    ul {
      list-style: none;
      padding-inline-start: 0;
      display: grid;
      margin: 0;
      @container (width >= 900px) {
        grid-template-columns: repeat(3, 1fr);
      }
    }
  }
</style>
