<div>
  <section aria-label="Instructor Profiles">
    <slot></slot>
  </section>
<div>

<style>
  @layer components {
    section {
      display: grid;
      gap: 2rem;
      @container (width >=900px) {
        grid-template-columns: repeat(2, minmax(200px, 1fr));
      }
    }
  }
</style>